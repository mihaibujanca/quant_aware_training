# @package _global_
experiment:
  name: autoencoder
  type: autoencoder

# Model architecture
model:
  hidden_sizes: [256, 128]
  latent_size: 32

# Dataset
data:
  name: mnist

# Training overrides
epochs: 15
learned_epochs: 15
batch_size: 256
